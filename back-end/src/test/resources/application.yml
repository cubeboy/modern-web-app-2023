spring:
  r2dbc:
    url: r2dbc:h2:mem:///testdb

logging:
  level:
    com:
      open:
        taskagile: DEBUG
    org:
      springframework:
        r2dbc: DEBUG

jwt:
  secretKey: ${SECRET_KEY}
  validityInMs: 36000
